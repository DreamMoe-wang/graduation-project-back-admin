<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.admin.mapper.TradePostReviewMapper">

    <insert id="insertTradePostReview" parameterType="com.example.admin.entity.TradePostReview" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO trade_post_review
        (post_id, reviewer_id, review_result, review_remark, create_time)
        VALUES
        (#{postId}, #{reviewerId}, #{reviewResult}, #{reviewRemark}, NOW())
    </insert>

</mapper>
